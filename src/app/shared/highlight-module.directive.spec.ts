import { HighlightModuleDirective } from './highlight-module.directive';

describe('HighlightModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightModuleDirective();
    expect(directive).toBeTruthy();
  });
});
